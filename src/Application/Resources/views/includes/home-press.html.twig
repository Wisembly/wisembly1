<div class="press">
    <div class="container">
        <h2>{{ 'press_love_us' | trans | raw }}</h2>
        {% for press in app.press_featured %}
            <a href="about#about-press" class="press-item">
                <img src="{{press.logo}}" />
            </a>
        {% endfor %}
    </div>
</div>
